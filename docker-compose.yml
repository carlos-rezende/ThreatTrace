version: '3.8'

services:
  threattrace:
    build: .
    ports:
      - "${PORT:-8090}:${PORT:-8090}"
    environment:
      - URLHAUS_AUTH_KEY=${URLHAUS_AUTH_KEY}
      - PORT=${PORT:-8090}
      - CACHE_TTL_SECONDS=3600
      - CACHE_MAX_SIZE=1000
    restart: unless-stopped
